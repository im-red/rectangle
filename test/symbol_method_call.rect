def Rectangle {
    enum LineType {
        Solid,
        Dot,
        Dash
    }
    int border.type: Solid
    void draw() {
        Rectangle r;
        r.draw();
        getDasharray(r.border.type);

        rect re;
        drawRect(re);

        list<int> l = {};
        list<int> m = {1, 2, 3};
        list<int> n = {r.x};

        string s = "123";
        string s2 = s;
        while (s == "123") {
            printString(s);
            while (s == "123") {
                printString(s);
                break;
            }
            break;
        }

        test();
        return;
    }
    string getDasharray(int type) {
        return "";
    }
    void test() {

    }
}