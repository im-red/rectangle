def Rectangle {
    enum LineType {
        Solid,
        Dot,
        Dash
    }
    int border.type: Solid
    void draw() {
        Rectangle r;
        r.draw();
        getDasharray(r.border.type);

        rect re;
        drawRect(re);

        string s = "123";
        printString(s);

        test();
    }
    string getDasharray(int type) {
        return "";
    }
    void test() {

    }
}