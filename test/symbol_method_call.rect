def Rectangle {
    enum LineType {
        Solid,
        Dot,
        Dash
    }
    int border.type: Solid
    void draw() {
        Rectangle r;
        r.draw();
        getDasharray(r.border.type);

        rect re;
        drawRect(re);

        test();
    }
    string getDasharray(int type) {
        return "";
    }
    void test() {

    }
}