def Rectangle {
    int x: 0
    int border.width: 1
    enum LineType {
        Solid,
        Dot,
        Dash
    }
    int border.type: Solid
    list<int> list0: {}
    list<int> test.list1: {}

    void listInit() {
        list<list<int>> points = {{1, 2}, {1, 2}, {1, 2}};
        list<int> l = {};
    }
    void stmt() {
        string s = "123";
        while (s == "123") {
            printString(s);
            while (s == "123") {
                printString(s);
                break;
            }
            break;
        }
    }
    void listSubscript() {
        list<list<int>> points = {{1, 2}, {1, 2}, {1, 2}};
        int x = points[0][0];
        int y = points[0][1];
        points[0][0] = points[0][1];
    }
    void draw() {
        Rectangle r;
        r.draw();
        getDasharray(r.border.type);

        rect re;
        drawRect(re);

        return;
    }
    string getDasharray(int type) {
        if (type == Solid) {
            return "Solid";
        }
        else if (type == Dot) {
            return "Dot";
        }
        else {
            return "Dash";
        }
        if (x == Solid) {
            return "Solid";
        }
        else if (border.type == Dot) {
            return "Dot";
        }
        else {
            return "Dash";
        }
    }
    void thisPropertyLvalue() {
        x = 0;
    }
    void thisGroupedPropertyLvalue() {
        border.width = 1;
    }
    void thisPropertyRvalue() {
        int a = x;
    }
    void thisGroupedPropertyRvalue() {
        int b = border.width;
    }
    void propertyLvalue() {
        Rectangle r;
        r.x = 0;
    }
    void groupedPropertyLvalue() {
        Rectangle r;
        r.border.width = 0;
    }
    void propertyRvalue() {
        Rectangle r;
        int a = r.x;
    }
    void groupedPropertyRvalue() {
        Rectangle r;
        int b = r.border.width;
    }
    void thisPropertyLvalueList() {
        list0[0] = 1;
    }
    void thisGroupedPropertyLvalueList() {
        test.list1[0] = 1;
    }
    void thisPropertyRvalueList() {
        int a = list0[0];
    }
    void thisGroupedPropertyRvalueList() {
        int b = test.list1[0];
    }
    void propertyLvalueList() {
        Rectangle r;
        r.list0[0] = 1;
    }
    void groupedPropertyLvalueList() {
        Rectangle r;
        r.test.list1[0] = 1;
    }
    void propertyRvalueList() {
        Rectangle r;
        int a = r.list0[0];
    }
    void groupedPropertyRvalueList() {
        Rectangle r;
        int b = r.test.list1[0];
    }
    void thisGroupedPropertyLRvalueList() {
        list0[1] = test.list1[0];
    }
    void groupedPropertyLRvalueList() {
        Rectangle r;
        r.list0[1] = r.test.list1[0];
    }
    void thisMethod(int foo) {
        thisMethod(x);
    }
    void instanceMethod(int foo) {
        Rectangle r;
        r.instanceMethod(r.x);
    }
}
